SELECT
    idhs14, SUM(CIFKG) AS CIFKG, WAKTU FROM `hs14` `t`
WHERE
    WAKTU >= '201501'
    AND WAKTU <= '201512'
    AND NEG_ASAL = 'JP'
GROUP BY WAKTU

SELECT
    idhs14, SUM(CIFKG) AS CIFKG, WAKTU FROM `hs14` `t`
WHERE
    WAKTU >= '201501'
    AND WAKTU <= '201512'
    AND NEG_ASAL = 'HK'
GROUP BY WAKTU

Done :
- Line chart in home.

Undone :
- Isian kolom 'dari tanggal' dan 'sampai tanggal'
- Dependencies dropdown list in validasi
- Select more than 1 choice (in negara asal, namaPelbong,jenis Komoditas, dan jenis komoditas HS) in validasi
- Add scatter plot
